[package]
name = "dbt-dist-classify"
version.workspace = true
homepage.workspace = true
repository.workspace = true
authors.workspace = true
description = "Locates dbt executables and classifies `dbt --version` output into a distribution and generation, with no dependency on dbt-common"
license.workspace = true
keywords.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
dbt-fusion-workspace-hack = { version = "0.1" }
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json = { workspace = true }
tempfile = { workspace = true }

[lib]
name = "dbt_dist_classify"
path = "src/lib.rs"
doctest = false
